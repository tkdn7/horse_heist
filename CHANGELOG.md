# 수정 기록

## 2026-09-08 — second fix 기반 게임 조작·표현 수정

### 요청과 실제 변경

- 말 선택 패널과 왼쪽 아래 연습 안내 카드를 제거했다. 연습 트랙의 E키 시작 기능은 유지했다.
- 좌우 이동 직후 오래된 서버 상태가 예측 레인을 덮어쓰는 문제를 수정했다. 입력 순번을 전송하고 서버가 처리한 순번 이후의 입력을 다시 적용한다. 일반 달리기, NPC 추격, 플레이어 추격 상태 모두에 적용했다.
- 좌우 이동 보간을 프레임 간격에 맞게 변경했다. 서버의 레인 판정과 이동·점프 제한은 유지했다.
- 말 달리기 반복음의 볼륨을 0.5에서 0.2로 낮췄다.
- 당근 수집 ID를 현재 달리기 동안 기억하고, 아직 화면에 생성되지 않은 당근의 수집 신호도 반영한다. 코스의 실제 rowLength로 지난 줄을 정리하여 긴 간격의 추격 코스에서 줄과 당근이 반복 생성되는 원인도 수정했다.
- 말 세 종류의 기존 관절을 사용해 네 다리의 착지 시점, 지면을 미는 동작, 발을 들어 회복하는 동작을 구분했다. 몸통의 들썩임을 줄이고 슬라이딩과 탑승자 로직은 유지했다.
- 양쪽 개인 농장을 도로 방향으로 회전했다. 전시칸과 귀환 위치도 같은 기준을 사용하며 중앙 출입구에 진입로를 추가했다. 입구 표지판은 통행을 막지 않도록 높였다.
- Place에 이미 저장된 생성 농장은 기존 건축 오브젝트를 함께 이동해 정렬한다. 생성된 FarmLane만 정렬하며 기존 월드 전체를 교체하지 않는다.

### 검증

- Rojo sourcemap 생성 및 Studio에서 변경된 7개 스크립트의 문법 검사 통과.
- 실제 Play에서 제거된 두 UI가 없는 것과 달리기 볼륨 0.2 확인.
- 지연된 서버 상태를 주입해 미확인 좌우 입력이 되돌아가지 않는 것 확인. 실제 달리기에서도 좌→우→우 입력 3건의 서버 확인과 최종 오른쪽 레인 확인.
- 80-stud 간격 코스에서 기존 줄을 유지하고 먹은 당근을 재생성하지 않는 것 확인. 생성 전 수집 신호를 받은 당근도 이후 생성되지 않고, 미수집 당근은 남는 것 확인.
- `tests/GameplayRegression.luau`를 Studio 테스트 클라이언트에서 실행하여 농장 12개 위치·144개 전시칸, 농장 정렬의 반복 실행, 당근 중복 보상 방지, 이동/점프 제한, 말 3종의 다리 위상과 슬라이딩 복원 검사 통과.
- 기존 `LobbyControls`의 공중 점프 방지 코드를 변경하지 않았으며 달리기 점프 우선순위 바인딩을 유지했다.

### 제한과 이어서 확인할 사항

- 원본 말은 다리가 한 덩어리인 모델이다. 무릎·발목 굽힘을 추가하려면 별도의 관절 모델 작업이 필요하다.
- 두 사람이 접속한 실제 네트워크 지연 환경의 플레이 감각은 추가 확인이 필요하다. 이번 검증은 Studio 1인 플레이와 지연 상태 주입으로 수행했다.
- 최초 테스트 도구에서 클라이언트 `loadstring` 실행이 제한되어, 같은 검증 코드를 직접 실행하는 방식으로 변경했고 검사를 통과했다.
- 변경 사항은 로컬 파일과 연결된 Studio 소스에 반영했다. GitHub push는 수행하지 않았다.
